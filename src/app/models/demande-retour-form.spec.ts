import { DemandeRetourForm } from './demande-retour-form';

describe('DemandeRetourForm', () => {
  it('should create an instance', () => {
    expect(new DemandeRetourForm()).toBeTruthy();
  });
});
